import Calendar from "../components/Admin/calendar";
import PageLayout from "../Hoc";

const CalendarView = () => {
  return (
    <PageLayout>
      <Calendar />
    </PageLayout>
  );
};

export default CalendarView;
